<div class="jumbotron">
  <h4>{{{strings.site_tagline}}}</h4>
  <br>
  <form role="form" class="gauge-form">
    <input type="password"
           class="form-control input-lg"
           name="password"
           placeholder="{{strings.gauge_password_placeholder}}"
           autocomplete="false"/>
    <div class="input-group input-group-sm">
      <select class="form-control" name="site">
        <option value="">Where do you use this?</option>
        {{#each hashing_algorithms.known_sites}}
          <option value="{{key}}">{{label}}</option>
        {{/each}}
      </select>
      <span class="input-group-addon">OR</span>
      <select class="form-control" name="hash">
        <option value="">How is your password secured?</option>
        {{#each hashing_algorithms.cryptographic_hash_functions}}
          <option value="{{key}}">{{label}}</option>
        {{/each}}
        {{#each hashing_algorithms.key_derivation_functions}}
          <option value="{{key}}">{{label}}</option>
        {{/each}}
      </select>
    </div>
  </form>
  <br>
  <p><a class="btn btn-lg btn-success" id="evaluate" href="#" role="button" title="{{strings.gauge_call_to_action}}">{{strings.gauge_call_to_action}}</a></p>
  <div class="results hide">
    <h4>{{strings.crack_time_section_label}}</h4>
    <ul class="list-group time-required"></ul>

    <h4>{{strings.metrics_section_label}}</h4>
    <ul class="list-group metrics"></ul>
  </div>
</div>

<div class="row marketing">
  <div class="col-lg-6">
    <h4>{{strings.gauge_faq_privacy_title}}</h4>
    <p>{{{strings.gauge_faq_privacy_body}}}</p>

    <h4>Subheading</h4>
    <p>Morbi leo risus, porta ac consectetur ac, vestibulum at eros. Cras mattis consectetur purus sit amet fermentum.</p>

    <h4>Subheading</h4>
    <p>Maecenas sed diam eget risus varius blandit sit amet non magna.</p>
  </div>

  <div class="col-lg-6">
    <h4>Subheading</h4>
    <p>Donec id elit non mi porta gravida at eget metus. Maecenas faucibus mollis interdum.</p>

    <h4>Subheading</h4>
    <p>Morbi leo risus, porta ac consectetur ac, vestibulum at eros. Cras mattis consectetur purus sit amet fermentum.</p>

    <h4>Subheading</h4>
    <p>Maecenas sed diam eget risus varius blandit sit amet non magna.</p>
  </div>
</div>